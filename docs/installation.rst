============
Installation
============

At the command line::

    $ easy_install tuvan_tools

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv tuvan_tools
    $ pip install tuvan_tools